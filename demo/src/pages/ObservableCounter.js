import React from 'react';

import {
  ObservableCounter as ObservableCounterDemo,
} from 'demos/Observable';

const ObservableCounter = () => (
  <div>
    <ObservableCounterDemo />
  </div>
);

export default ObservableCounter;
